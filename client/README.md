# Client setup

## Install
npm install

## Run locally
npm run dev

## Build
npm run build

## Environment
Create a `.env` file in `client/` with:

VITE_API_URL=http://localhost:5000

For Render, point this to your deployed backend URL.
